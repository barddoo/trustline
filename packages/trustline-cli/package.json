{
  "name": "trustline-cli",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun run scripts/build-cli.mjs host",
    "build:release": "bun run scripts/build-cli.mjs release",
    "check": "biome check .",
    "cli": "bun src/main.ts",
    "test": "bunx vitest run tests/cli.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "citty": "^0.2.1",
    "trustline": "workspace:*"
  }
}
